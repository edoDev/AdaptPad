find replace
mode to change between pretty format and "code" backend side so you can see the mk changes underneath
paragraph mode isn't working
export table did not format correctly.
ability to convert pictures to text. 
